/**
 * General constants used across multiple features
 */

// ============================================================================
// Pagination defaults
// ============================================================================

/**
 * Default page number for paginated queries
 */
export const DEFAULT_PAGE = 1;

/**
 * Default page size/limit for paginated queries
 */
export const DEFAULT_PAGE_SIZE = 20;
